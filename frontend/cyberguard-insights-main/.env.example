# Node Environment
NODE_ENV=production

# Application Port (for local development)
PORT=8080

# Optional API Backend URL (if connecting to an external Flask prediction service)
# VITE_API_BASE_URL=https://api.yourdomain.com

# Application Title
VITE_APP_TITLE=CipherTrace - CyberShield AI
